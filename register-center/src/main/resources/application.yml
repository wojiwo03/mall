server:
  port: 7356

spring:
  application:
    name: register-center

eureka:
  instance:
    hostname: localhost
    preferIpAddres: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    healthcheck:
      enabled: false
    serviceUrl:
      defaultZone: http://localhost:7356/eureka/